# 开发流程

1. 深度研究用于技术选型、规范研究、代码审计和重大重构，不直接参与运行时生成。
2. 研究结论写入 `docs/research`，确认后的决策写入 `docs/adr`。
3. 每个模块使用功能分支，先运行后端单元/集成测试和前端构建，再提交 PR。
4. PR 必须检查：不读旧 Markdown、不提交客户资料/数据库/密钥、发布隔离、DOCX 导出和视觉回归。
5. `main` 只保留可运行的稳定版本；本地知识资产和交付文件不进入 GitHub。
