# 非人工任务完成报告（2026-08-09）

## 结论

计划中可以由代码、规则、模型或自动化测试完成的工作已经形成可运行闭环。当前生产栈使用 PostgreSQL、Redis、Qdrant、MinIO、独立 embedding/reranker 服务、Dramatiq worker 和可选 Prometheus/Grafana；应用数据库迁移版本为 `20260809_02`。

本报告只确认技术实现和自动化验收通过，不把 AI 自检等同于总工签审。剩余事项均需要责任人对来源、风险或正式交付承担判断责任。

## 本轮完成内容

- 为白银评测增加独立 AI 复核状态、置信度、问题清单和运行血缘，并提供后台任务、批量审核接口及前端工作台。
- 修正困难负样本定义，增加检索探测与自动重建；不再使用“缺少精确数值”或“相反结论”作为错误负样本证明检索质量。
- 混合检索增加 reranker 最低分阈值，并记录 FTS 基线负样本拒绝率和提升值。
- 增加可返回非零退出码的自动验收脚本，固定 Recall、NDCG、负样本拒绝、基线提升和审计链门槛。
- 固定 `qdrant-client 1.13.x` 与 Qdrant `1.13.4` 的兼容组合。
- 扩展 Prometheus 指标并提供预置 Grafana 面板。
- 扩充 GitHub Actions：后端、前端、PostgreSQL 迁移和 Compose 服务健康检查。
- 将 `pypdf` 升级到 `6.15.0`、`requests` 升级到 `2.33.0`，依赖漏洞扫描为 0 个已知漏洞。

## 自动验收证据

最终重放使用数据集 `live-20260809-v2`，评测运行 ID 为 6：

| 指标 | 结果 | 门槛 |
|---|---:|---:|
| 样本数 | 12 | - |
| 正样本 / 困难负样本 | 9 / 3 | - |
| Recall@10 | 1.0000 | >= 0.80 |
| MRR | 1.0000 | 记录项 |
| NDCG@10 | 1.0000 | >= 0.75 |
| 混合检索负样本拒绝率 | 1.0000 | >= 0.80 |
| FTS 负样本拒绝率 | 0.3333 | 基线 |
| 负样本拒绝提升 | 0.6667 | > 0 |
| 审计链 | 有效，121 个事件 | 必须有效 |

其他验证结果：

- 后端测试：35 项通过。
- 前端生产构建：通过；`npm audit --audit-level=high` 为 0 个漏洞。
- Playwright：登录、运行状态、评测工作台及 390 像素移动视图共 4 项通过。
- Python 依赖扫描：0 个已知漏洞。
- Compose：应用、worker、PostgreSQL、Redis、Qdrant、MinIO、模型服务、Prometheus 和 Grafana均正常运行。
- Grafana 已发现预置面板 `bid-writer-overview`，Prometheus 可采集 `bid_writer_audit_chain_valid=1`。

## 备份恢复证据

备份 `backups/20260809-073644` 的 4 项校验和全部通过。`postgres.dump` 已恢复到临时空数据库并与源库逐项核对：

- 知识单元：259
- 检索评测样本：26
- 检索评测运行：5（备份时点）
- 审计事件：105（备份时点）
- Alembic：`20260809_02`

PostgreSQL 核对完全一致，Qdrant 与 MinIO 归档均存在；验证后的临时数据库已删除。后续评测运行和审计事件是备份之后新增，生产备份应继续按计划滚动执行。

最终状态另生成滚动备份 `backups/20260809-075433`，其中 `env.backup`、`postgres.dump`、`qdrant.tar.gz` 和 `minio.tar.gz` 的 4 项 SHA-256 校验全部通过。该备份包含评测运行 ID 6 和最新审计事件；空环境恢复流程已由上一备份完成验证。

## 剩余人工事项

- 候选 61、69、92 的 `auto_publish_sample` 抽检：核对原文忠实性和跨项目适用性。
- 候选 49 的独立裁决失败复核。
- 当前 21 个 `ready` 候选均为中风险，必须由人确认后才能接受；高风险、模型分歧及来源异常候选继续保留在待办中。
- 从 `live-20260809-v2` 选择代表性问题提升为黄金集。AI 白银集可用于工程回归，但不能自行证明业务正确性。
- 两套脱敏演练夹具的自动端到端测试已经通过；真实医院/学校、河道治理项目仍需提供项目资料，并由责任人签审高风险 Claim 和最终 DOCX/PDF。
