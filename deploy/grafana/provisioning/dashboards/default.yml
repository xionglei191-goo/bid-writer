apiVersion: 1

providers:
  - name: Bid Writer
    orgId: 1
    folder: Bid Writer
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
